<!DOCTYPE html>
<html>

<head>
  <title>
    <%= content_for(:title) || "G-Tarefa" %>
  </title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="mobile-web-app-capable" content="yes">
  <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
      <%= yield :head %>

        <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
          <%#= tag.link rel: "manifest" , href: pwa_manifest_path(format: :json) %>

            <link rel="icon" href="/icon.png" type="image/png">
            <link rel="icon" href="/icon.svg" type="image/svg+xml">
            <link rel="apple-touch-icon" href="/icon.png">

            <%# Includes all stylesheet files in app/assets/stylesheets %>
            <%= stylesheet_link_tag "application", media: "all", data: { turbo_track: "reload" } %>

                <%= javascript_importmap_tags %>

</head>

<body>
  <div class="d-flex h-100 align-items-center justify-content-center row mx-auto"
    style="background-image: url('<%= asset_path("bg.jpg") %>');">
    <main class="col-11 col-md-10 col-lg-8 shadow-lg p-5 mb-5 bg-white rounded">

      <% flash.each do |type, message| %>
        <div class="alert alert-warning alert-dismissible fade show" role="alert">
          <%= message %>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
        <% end %>

          <%= yield %>
    </main>
  </div>

</body>
</html>